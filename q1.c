#include <stdio.h>

int main()
{
    // maxium of two number
    int a, b;
    scanf("%d%d",&a,&b);
    if(a > b)
    {
       printf("sghs %d",a);
    }
    else
    {
        printf("%d",b);
    }
}